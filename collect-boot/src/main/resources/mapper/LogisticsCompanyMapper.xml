<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tzj.collect.mapper.LogisticsCompanyMapper">

<select id="selectLogisticeCompanyIds" parameterType="Integer" resultType="Integer">
    SELECT DISTINCT
        a.logistics_id
        FROM
        sb_logistics_company_area a
        LEFT JOIN sb_logistics_company_category b ON a.logistics_id = b.logistics_id
        WHERE
        a.stree_id = #{streeId}
    AND b.parent_id = #{categoryId}
    AND a.del_flag = 0
    AND b.del_flag = 0
</select>



</mapper>